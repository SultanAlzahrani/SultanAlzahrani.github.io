// Styles
import "../styles/TimeLine.css";

// TimeLine(TL) Section Title
function TLSectionTitle({ title }) {
  return (
    <section className="section-header">
      <span className="section-title">{title}</span>
    </section>
  );
}
